# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code per-project files
.claude/
CLAUDE.md
AGENTS.md

# Internal project docs — design history, not part of the published app
.project/

# HRV reference clone — consultation only, never committed
.reference/

# Pake desktop build artifacts (built in CI; never commit local runs)
*.dmg
*.msi
*.AppImage
